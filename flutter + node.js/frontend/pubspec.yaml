name: py_sync
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.3.0
  bloc_suite: ^0.0.7
  flutter_bloc: ^9.1.0
  retry: ^3.1.2
  hydrated_bloc: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.0
  build_runner: ^2.3.3

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
